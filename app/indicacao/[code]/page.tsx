"use client";

// Rota antiga — mantida por compatibilidade com links já compartilhados
// antes da mudança pra URL curta (/r/CODIGO). Novos links usam /r/[code];
// esta rota não é mais gerada, só consumida se alguém já tiver o link salvo.
export { ReferralLandingPage as default } from "@/components/referral-landing";
